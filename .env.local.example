NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# Optional. The site's canonical URL (used for metadata, sitemap, robots.txt,
# JSON-LD, etc.) is auto-detected from Vercel's own production domain -- see
# src/lib/site.ts. Only set this if you need to force a specific value.
# NEXT_PUBLIC_SITE_URL=https://your-domain.com
